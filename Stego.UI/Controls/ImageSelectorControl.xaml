<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Stego.UI.Controls.ImageSelectorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stego.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Border
        Background="{ThemeResource ControlFillColorTertiaryBrush}"
        CornerRadius="8"
        Padding="4"
        >
        <Grid>
            <Image x:Name="CoverImage" Stretch="Uniform" Visibility="Collapsed"/>
            
            <Button 
                x:Name="CoverImageSelectionButton" 
                Content="Select Cover Image" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Click="OnSelectImageClicked"/>

            <StackPanel 
                x:Name="CoverImageActionPanel"
                Orientation="Horizontal" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="12"
                Visibility="Collapsed"
                Spacing="8">

                <Button Click="OnRemoveImageClicked">
                    <FontIcon Glyph="&#xE894;" FontSize="12" />
                </Button>
            </StackPanel>
            
        </Grid>
    </Border>
    
</UserControl>
