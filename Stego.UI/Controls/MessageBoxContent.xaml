<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Stego.UI.Controls.MessageBoxContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stego.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid>
        <Border
            Padding="20"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            CornerRadius="8">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <FontIcon x:Name="ErrorIcon" Grid.Column="0" Glyph="&#xEB90;" Visibility="Collapsed"/>
                    <FontIcon x:Name="WarningIcon" Grid.Column="0" Glyph="&#xE814;" Visibility="Collapsed"/>
                    <FontIcon x:Name="InfoIcon" Grid.Column="0" Glyph="&#xE783;" Visibility="Visible"/>
                    <TextBlock
                        Grid.Column="1"
                        Text="{Binding Message, ElementName=root}"
                        TextWrapping="Wrap"
                        FontSize="14"
                        Margin="0,0,0,10"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
    
</UserControl>
